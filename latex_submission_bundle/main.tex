\documentclass[11pt]{article}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{microtype}
\usepackage{natbib}
\usepackage{xcolor}
\usepackage{placeins}
\usepackage[colorlinks=true,allcolors=blue]{hyperref}
\graphicspath{{figures/main/}{figures/appendix/}}
\input{results_macros}

\title{Multiplicity-Corrected Inverse Probability Scaling for
Reward-Proportional Sampling in Large DAGs}
\author{}
\date{}

\begin{document}
\maketitle

\begin{abstract}
Many scientific generation problems require sampling diverse, high-reward
outcomes rather than finding a single maximizer. Count-based inverse probability
scaling (IPS) estimates terminal probability from repetitions within a sampled
group. When the terminal space is large relative to the group, repetitions
become rare and the correction becomes nearly constant, reducing the update
toward GRPO. Replacing terminal probability with trajectory probability does
not solve this problem in a directed acyclic graph: terminals reachable by many
trajectories receive excess weight. We introduce MIPS-GRPO, which uses the
trajectory weight \(R(x)q(\tau\mid x)/P_F(\tau)\), where \(q\) is a normalized
reverse allocation over paths to \(x\). This allocation makes the total expected
weight assigned to each terminal equal to \(R(x)\), independent of path
multiplicity. In a controlled hypergrid, count IPS recovers all modes when
repetitions are common but collapses when they are sparse; MIPS-GRPO recovers all
modes in both regimes. In the matched five-taxon experiment, GRPO covers 1 of
105 topologies, IPS-GRPO covers 3, PhyloGFN covers 15, and MIPS-GRPO covers all
105. In molecular synthesis, MIPS-GRPO produces \(93.59\pm2.43\%\) unique valid
samples, compared with \(74.54\pm3.99\%\) for RGFN and less than 0.2\% for GRPO
and IPS-GRPO. These results indicate that multiplicity correction can reduce
mode collapse while preserving reward quality.
\end{abstract}

\section{Introduction}

Drug design, program synthesis, and Bayesian inference often have many useful
answers. For such problems, returning only the highest-reward terminal is less
useful than representing the full reward-weighted set. The desired target is
\begin{equation}
 p^*(x)=\frac{R(x)}{Z}, \qquad Z=\sum_x R(x).
 \label{eq:target}
\end{equation}
Expected-return maximization does not target Eq.~\eqref{eq:target}; it rewards
moving probability toward the best terminal and therefore encourages
outcome-level collapse. IPS cancels the terminal-frequency factor in the
policy gradient by dividing reward by terminal probability
\citep{sinha2026ips}.

Count IPS-GRPO estimates terminal probability from repeats inside a group of
\(G\) samples. If the effective terminal space \(K\) is much larger than
\(G\), almost every observed terminal appears once and \(\hat p(x)=1/G\).
Group normalization removes the common factor, leaving essentially the GRPO
update.

A sampled trajectory has a known probability \(P_F(\tau)\), suggesting the
weight \(R(x)/P_F(\tau)\). This works in a tree but not a DAG. If terminal
\(x\) has \(m(x)\) construction paths, that weight assigns it total mass
\(m(x)R(x)\), confounding reward with reachability. This is central in both
applications studied here: a phylogenetic tree has many merge orders and a
molecule may have many synthesis trajectories.

We evaluate the method in three settings. A controlled hypergrid separates a
regime with informative counts from one with sparse counts. Phylogenetic
inference tests a combinatorial space in which many merge orders lead to the
same tree. Molecular synthesis tests a different construction graph and a
separate flow-based baseline.

\paragraph{Contributions.}
\begin{itemize}
 \item We characterize the repeat limitation of count IPS through the ratio
 between the effective terminal space and the sampled group size.
 \item We derive a normalized reverse-allocation correction that removes path
 multiplicity without requiring repeated terminal outcomes.
 \item We evaluate the correction in informative-count and sparse-count
 hypergrid regimes against GRPO, count IPS-GRPO, and GFlowNet TB.
 \item We study the same effect in phylogenetic inference and molecular
 synthesis using matched four-method comparisons.
\end{itemize}

\section{Multiplicity-corrected IPS}

Let a forward policy generate trajectory
\(\tau=(s_0,a_1,\ldots,a_T,s_T)\) and terminal \(x=s_T\). Its path
probability is known,
\begin{equation}
 P_F(\tau)=\prod_{t=1}^{T}P_F(a_t\mid s_{t-1}),
\end{equation}
but the terminal probability is a sum over paths,
\begin{equation}
 P_F(x)=\sum_{\tau\in\mathcal T(x)}P_F(\tau).
 \label{eq:marginal}
\end{equation}
Equation~\eqref{eq:marginal} becomes intractable in large DAGs.

\subsection{Why naive path IPS is wrong}

For \(w_{\mathrm{naive}}(\tau)=R(x)/P_F(\tau)\), the expected weighted mass
assigned to terminal \(x\) is
\begin{equation}
 \sum_{\tau\in\mathcal T(x)}P_F(\tau)w_{\mathrm{naive}}(\tau)
 =|\mathcal T(x)|R(x).
\end{equation}
The desired reward is multiplied by path count. This is structural bias, not
merely high variance.

\subsection{Reverse allocation across paths}

Let \(q(\tau\mid x)\) be any normalized distribution over paths terminating
at \(x\): \(\sum_{\tau\in\mathcal T(x)}q(\tau\mid x)=1\). Define
\begin{equation}
 w(\tau)=\frac{R(x)q(\tau\mid x)}{P_F(\tau)}.
 \label{eq:mips}
\end{equation}
Then
\begin{equation}
 \sum_{\tau\in\mathcal T(x)}P_F(\tau)w(\tau)
 =R(x)\sum_{\tau\in\mathcal T(x)}q(\tau\mid x)=R(x).
 \label{eq:unbiased}
\end{equation}
The normalization of \(q\) is enough for Eq.~\eqref{eq:unbiased}; a learned
reverse model is not required. A uniform backward policy is valid. If
\(q(\tau\mid x)=P_F(\tau\mid x)\), the weight becomes the ideal
\(R(x)/P_F(x)\) and is constant across paths to the same terminal. Learning a
reverse model may reduce variance, but that is an empirical benefit rather
than a correctness requirement.

\subsection{Policy update and relationship to GFlowNets}

For each on-policy batch we sample trajectories, score them with a frozen
reverse policy, form
\begin{equation}
 \log w_i=\log R(x_i)+\log q(\tau_i\mid x_i)-\log P_F(\tau_i),
\end{equation}
normalize weights into advantages, and take a path-level PPO update. When
\(q=q_\phi\) is fitted, maximum likelihood on observed reverse edges is
performed only after scoring the batch. This order avoids using the same batch
to increase its own weight.

GFlowNets also target reward-proportional terminal distributions
\citep{bengio2021gflownet,bengio2023foundations}. Trajectory balance uses a
backward policy and learned partition function in a squared balance residual
\citep{malkin2022tb}; MIPS-GRPO uses the backward allocation inside an
importance weight and updates the forward policy with PPO.

\input{tables/methods}

\section{Controlled evaluation: informative and sparse counts}

\subsection{Hypergrid and two regimes}

We use a two-dimensional hypergrid with \(H^2\) terminals and four symmetric
high-reward modes. Starting at \((0,0)\), each action increments one coordinate
or terminates. The reward landscape follows \citet{sinha2026ips}, with
\(R_0=0.1\), \(R_1=0.5\), and \(R_2=2.0\). Because
\(p(x)\propto R(x)\) is enumerable, we measure exact empirical \(\ell_1\)
distance to target, modes recovered, and terminal coverage.

At \(H=8,G=256\), there are 64 terminals and repeats are plentiful
(\(K/G=0.25\)). At \(H=64,G=32\), there are 4096 terminals and repeats are
rare (\(K/G=128\)). The latter is also a multiplicity stress test: terminal
\((a,b)\) has \(\binom{a+b}{a}\) paths, and the four equal-reward modes differ
in path count by roughly 24 orders of magnitude.

\input{tables/hypergrid_main}

\begin{figure}[htbp]
 \centering
 \includegraphics[width=\textwidth]{figure1_hypergrid_regimes.pdf}
 \caption{\textbf{The count estimator succeeds only while outcomes repeat.}
 Left: exact \(\ell_1\) distance to target. Right: modes recovered. Small-grid
 bars show mean and one standard deviation over three seeds; the large-grid
 suite is one seed. GFlowNet TB was run only in the large-grid suite.}
 \label{fig:hypergrid-summary}
\end{figure}

\begin{figure}[htbp]
 \centering
 \includegraphics[width=\textwidth]{figure2_hypergrid_h8_distributions.png}
 \caption{\textbf{Positive control: counting works when outcomes repeat.}
 Terminal distributions for \(H=8,G=256\) (seed 0). GRPO collapses, count IPS
 recovers all four modes, and MIPS-GRPO most closely matches the target.}
 \label{fig:h8-distributions}
\end{figure}

\begin{figure}[htbp]
 \centering
 \includegraphics[width=\textwidth]{figure3_hypergrid_h64_distributions.png}
 \caption{\textbf{Stress test: counting fails without repeats.} Terminal
 distributions for \(H=64,G=32\) (seed 0). GRPO and count IPS-GRPO recover one
 mode; MIPS-GRPO and GFlowNet TB recover all four.}
 \label{fig:h64-distributions}
\end{figure}

\subsection{Results}

When counts are informative, count IPS-GRPO lowers \(\ell_1\) from
\(1.768\) for GRPO to \(\SmallGridCountLone\) and recovers all modes. Count IPS
works here because outcomes repeat within each group. MIPS-GRPO also recovers
all modes and reaches \(\SmallGridMIPSLone\).

When counts are sparse, GRPO and count IPS-GRPO each recover one mode, with
\(\ell_1=\LargeGridGRPOLone\) and \(\LargeGridCountLone\). MIPS-GRPO visits
4056 of 4096 terminals, recovers all modes, and reaches
\(\ell_1=\LargeGridMIPSLone\). GFlowNet TB also recovers all modes and reaches
\(\LargeGridTBLone\). MIPS-GRPO and TB have nearly the same error in this seed.

\begin{figure}[htbp]
 \centering
 \includegraphics[width=\textwidth]{figure4_hypergrid_failure_diagnostics.png}
 \caption{\textbf{More samples do not fix the collapsed policies.}
 Left: GRPO and count IPS do not find the missing modes after 50,000 samples.
 Right: they still achieve high mean reward, so mean reward alone does not
 measure distribution matching.}
 \label{fig:hypergrid-diagnostics}
\end{figure}

GRPO and count IPS still miss modes after 50,000 samples. MIPS-GRPO and TB find
all four modes within roughly \(10^2\) samples
(Figure~\ref{fig:hypergrid-diagnostics}). The collapsed policies can still have
high mean reward, so reward alone is not enough to measure distribution fit.

\FloatBarrier

\section{Application I: Bayesian phylogenetic inference}

A phylogenetic tree is built by repeatedly merging subtrees. The same tree can
be built in many merge orders. A terminal signature contains its topology and
branch lengths. We compare against PhyloGFN, a task-specific trajectory-balance
baseline \citep{zhou2024phylogfn}.

We first use the five-taxon DS1 reduced benchmark, where all 105 unrooted
binary topologies can be enumerated. The matched no-replay evaluation compares
GRPO, within-group count IPS-GRPO, MIPS-GRPO, and PhyloGFN using one million
final-policy trajectories per method. GRPO collapses to one signature and one
topology. IPS-GRPO improves signature support to 66,268 but covers only 3 of
105 topologies. MIPS-GRPO covers all 105 topologies and retains 951,180 distinct
signatures; PhyloGFN covers 15 topologies and retains 100,508. Their log--log
correlations are similar (0.992 and 0.991), but their topology coverage is not.

\input{tables/phylo_main}

\begin{figure}[htbp]
 \centering
 \includegraphics[width=\textwidth]{figure5_phylo_four_methods.pdf}
 \caption{\textbf{Matched four-method phylogenetic comparison at five taxa.}
 GRPO, count-based IPS-GRPO, MIPS-GRPO, and PhyloGFN are evaluated without
 replay using one million final-policy trajectories each. MIPS-GRPO is the only
 method to cover all 105 exact topologies and retains 95.1\% distinct terminal
 signatures. PhyloGFN has nearly the same correlation but covers 15
 topologies.}
 \label{fig:phylo-four-methods}
\end{figure}

\FloatBarrier

\section{Application II: full-space molecular synthesis}

We test a chemically structured construction DAG using the sEH proxy task.
Each method receives 2,500 policy updates (250,000 forward trajectories) and
50,000 independent final-policy samples for each of three seeds. We compare
GRPO, count IPS-GRPO, MIPS-GRPO, and RGFN.

MIPS-GRPO produces \(93.59\pm2.43\%\) unique valid samples, compared with
\(74.54\pm3.99\%\) for RGFN, \(0.011\pm0.010\%\) for GRPO, and
\(0.173\pm0.286\%\) for count IPS-GRPO. MIPS-GRPO and RGFN have similar mean
sampled proxy values (\(4.281\pm0.177\) and \(4.227\pm0.421\)) and discover
\(22.3\pm17.0\) and \(14.0\pm9.2\) separated high-proxy modes, respectively.
Neither GRPO baseline finds a mode above the threshold in any seed. Mode counts
vary across the three seeds, so we do not claim a clear difference between
MIPS-GRPO and RGFN on this metric.

\input{tables/molecule_main}

\begin{figure}[htbp]
 \centering
 \includegraphics[width=\textwidth]{figure6_molecule_final_2500.pdf}
 \caption{\textbf{Full-space sEH final-policy comparison.} Each method was
 trained for 2,500 updates and evaluated with 50,000 independent samples for
 each of three seeds. Bars show the arithmetic mean across seeds, whiskers show
 \(\pm1\) sample standard deviation, and circles show individual seeds. Modes
 have sEH proxy above 7 and maximum Tanimoto similarity at most 0.5 under
 greedy leader selection. Lower uniqueness whiskers are clipped to the display
 floor of the logarithmic panel.}
 \label{fig:molecule-final}
\end{figure}

\FloatBarrier

\section{Summary of findings}

Across the controlled and applied experiments, the results follow the expected
effect of count sparsity. Count IPS is effective when a sampled group contains
repeated outcomes, but its correction becomes nearly constant as repetitions
disappear. MIPS-GRPO instead uses path probabilities and a normalized reverse
allocation, allowing it to retain broader terminal coverage without relying on
counts. The comparisons with GFlowNet TB, PhyloGFN, and RGFN support
multiplicity correction as a practical way to reduce collapse, rather than a
claim of universal dominance over flow-based methods.

\section{Limitations and conclusion}

The controlled study evaluates two values of \(K/G\); a denser sweep would
characterize the transition more precisely. The phylogenetic correlations are
self-sampled diagnostics, so a shared fixed-tree benchmark would provide a
stronger cross-method comparison. Equation~\eqref{eq:mips} assumes on-policy
data; replay requires an additional correction, and rewards must be positive.
The 10-taxon ablation also gives similar effective sample size for fitted and
uniform reverse policies, leaving the empirical benefit of learning
\(q_\phi\) unresolved.

Within these conditions, MIPS-GRPO provides a count-free correction for
reward-proportional sampling when outcomes rarely repeat and multiple paths
reach the same terminal. The experiments consistently associate this correction
with improved terminal coverage and reduced mode collapse.

\clearpage
\appendix
\setcounter{figure}{0}
\setcounter{table}{0}
\renewcommand{\thefigure}{A\arabic{figure}}
\renewcommand{\thetable}{A\arabic{table}}
\renewcommand{\theHfigure}{appendix.\arabic{figure}}
\renewcommand{\theHtable}{appendix.\arabic{table}}

\section{Per-seed hypergrid results}

The appendix contains per-seed values, small-scale controls, training curves,
and additional diagnostics.

\input{tables/hypergrid_per_seed}

\begin{figure}[htbp]
 \centering
 \includegraphics[width=\textwidth]{figureS1_hypergrid_h8_distributions.png}
 \caption{Terminal distributions for the small-grid positive-control regime,
 \(H=8,G=256\) (seed 0). This raster was recovered from the previous compiled
 draft because the original figure link pointed to another machine. Exact
 values are in the accompanying tables and CSV files.}
\end{figure}

\begin{figure}[htbp]
 \centering
 \includegraphics[width=\textwidth]{figureS2_hypergrid_h64_distributions.png}
 \caption{Terminal distributions for the large-grid stress test,
 \(H=64,G=32\) (seed 0). GRPO and count IPS-GRPO recover one mode; MIPS-GRPO
 and GFlowNet TB recover all four.}
\end{figure}

\FloatBarrier
\section{Additional phylogenetic diagnostics}

The main paper reports the matched four-method five-taxon comparison. Here we
retain the older five- and ten-taxon probability--reward views,
matched-transform diagnostics, and training curves for context.

\begin{figure}[htbp]
 \centering
 \includegraphics[width=\textwidth]{figureS4_phylo_5taxa_legacy_scatter.png}
 \caption{\textbf{Five-taxon probability--reward scatter retained for visual
 context.} This older four-method panel was generated under a mixed legacy
 protocol (including a replay-based GFlowNet artifact), so it is not the source
 of the matched no-replay numbers above and should not be used for quantitative
 ranking.}
\end{figure}

\begin{figure}[htbp]
 \centering
 \includegraphics[width=\textwidth]{figureS5_phylo_10taxa_four_methods.png}
 \caption{\textbf{Ten-taxon scaling diagnostic.} The older four-method
 probability--reward view shows the collapse signature for GRPO and count IPS
 and broad sampled support for the multiplicity-aware methods. Because methods
 sample different terminal sets, this is a within-policy diagnostic rather
 than a shared-outcome benchmark.}
\end{figure}

\begin{figure}[htbp]
 \centering
 \includegraphics[width=\textwidth]{figureS6_phylo_10taxa_matched_transform.png}
 \caption{\textbf{Ten-taxon matched-transform view.} Linear and log--log
 transforms are shown consistently across methods. Interpret each correlation
 as a property of that policy's self-sampled support; a fixed-tree comparison
 on identical outcomes remains pending.}
\end{figure}

\FloatBarrier
\section{Reverse-policy diagnostics}

\input{tables/phylo_diagnostic}
\input{tables/reverse_ablation}

\begin{figure}[htbp]
 \centering
 \includegraphics[width=\textwidth]{figureS8_reverse_training.png}
 \caption{Reverse-policy training curves at 5, 10, and 27 taxa. These curves
 show training behavior, not final distribution fit.}
\end{figure}

\FloatBarrier
\section{Molecular-generation per-seed results}

The aggregate molecular result in the main paper is based on the following 12
final-policy evaluations. These values show the substantial seed-to-seed
variation in mode discovery that motivates our descriptive interpretation.

\input{tables/molecule_per_seed}

\begin{figure}[htbp]
 \centering
 \includegraphics[width=\textwidth]{figureS6_molecule_final_2500.pdf}
 \caption{Full-size copy of the three-seed full-space sEH comparison. Bars show
 arithmetic means, whiskers show \(\pm1\) sample standard deviation, and circles
 show individual seeds.}
\end{figure}

\FloatBarrier
\clearpage
\section{Training and additional diagnostics}

The following plots document optimization behavior and stricter diagnostics.
They are retained as supporting evidence rather than used as primary
distributional results.

\begin{figure}[htbp]
 \centering
 \includegraphics[width=\textwidth]{figureS8_hypergrid_training_l1.png}
 \caption{Evaluation \(\ell_1\) during training in both hypergrid regimes.
 Panels have different scales and should not be compared numerically across
 grid sizes. The final contrasts are stable rather than isolated checkpoint
 fluctuations.}
\end{figure}

\begin{figure}[htbp]
 \centering
 \includegraphics[width=\textwidth]{figureS9_phylo_5taxa_topology_ground_truth.png}
 \caption{Five-taxa topology-level diagnostic against the exact 105-topology
 target. This test is stricter than self-sampled signature correlation and
 exposes topology-level differences hidden by pathwise fits.}
\end{figure}

\begin{figure}[htbp]
 \centering
 \includegraphics[width=\textwidth]{figureS10_phylo_training_5taxa_mips.png}
 \caption{MIPS-GRPO optimization diagnostics at five taxa.}
\end{figure}

\begin{figure}[htbp]
 \centering
 \includegraphics[width=\textwidth]{figureS11_phylo_training_5taxa_tb.png}
 \caption{GFlowNet trajectory-balance optimization diagnostics at five taxa.}
\end{figure}

\begin{figure}[htbp]
 \centering
 \includegraphics[width=\textwidth]{figureS12_phylo_training_10taxa_mips.png}
 \caption{MIPS-GRPO optimization diagnostics at ten taxa.}
\end{figure}

\begin{figure}[htbp]
 \centering
 \includegraphics[width=\textwidth]{figureS13_phylo_training_10taxa_tb.png}
 \caption{GFlowNet trajectory-balance optimization diagnostics at ten taxa.}
\end{figure}

\begin{figure}[htbp]
 \centering
 \includegraphics[width=\textwidth]{figureS14_phylo_training_27taxa_mips.png}
 \caption{Legacy MIPS-GRPO optimization diagnostics at 27 taxa. The dashed
 line marks 10,000 epochs.}
\end{figure}

\begin{figure}[htbp]
 \centering
 \includegraphics[width=\textwidth]{figureS15_phylo_training_27taxa_tb.png}
 \caption{Legacy GFlowNet training curves at 27 taxa. These curves are kept for
 context and are not part of the main results.}
\end{figure}

\FloatBarrier
\bibliographystyle{plainnat}
\bibliography{references}
\end{document}
